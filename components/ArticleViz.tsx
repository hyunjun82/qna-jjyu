"use client";

/**
 * ArticleViz – 슬러그별 시각화 컴포넌트 렌더러
 * 현재는 빈 stub. 추후 시각화 데이터 추가 시 구현.
 */
export function ArticleViz({
  slug,
  position,
}: {
  slug: string;
  position: string;
}) {
  // TODO: 시각화 매핑 데이터가 추가되면 여기서 렌더링
  return null;
}
